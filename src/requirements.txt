# =============================================================================
# LAMBDA PRODUCTION DEPENDENCIES (minimal for deployment)
# =============================================================================

# FastAPI and core dependencies
fastapi~=0.116
pydantic~=2.11
pydantic-settings~=2.10
email-validator~=2.2

# Lambda adapter for ASGI applications
mangum==0.19.0

# Database dependencies (only what's needed for Lambda)
pynamodb~=6.1

# Dependency injection
injector==0.22.0
fastapi-injector==0.8.0
